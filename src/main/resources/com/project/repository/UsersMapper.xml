<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.repository.UsersMapper">
	<select id="login" resultType="_int">
		SELECT COUNT(*) FROM USERS WHERE USERNAME=#{username} AND USERSPASSWORD=#{userspw}
	</select>
		
	<select id="getUsers" resultType="Users">
		SELECT * FROM USERS WHERE USERNAME=#{username}
	</select>
	
	<insert id="join">
		INSERT INTO USERS
		(USERSNUMBER, USERNAME, USERSPW, USERSNICKNAME, USERSEMAIL, ROLE)
		VALUES(seq_users.nextVal, #{username}, #{userspw}, #{usersnickname}, #{usersemail}, #{role})
	</insert>
	
	<insert id="joinOAuth">
		INSERT INTO USERS
		(USERSNUMBER, USERNAME, USERSPW, USERSNICKNAME, USERSEMAIL, ROLE, PROVIDER, PROVIDERID)
		VALUES(seq_users.nextVal, #{username}, #{userspw}, #{usersnickname}, #{usersemail}, #{role}, #{provider}, #{providerid})
	</insert>
</mapper>